spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
    format:
      date: iso
      date-time: iso
      time: iso
  application:
    name: flight-search-service


management:
  endpoints:
    web:
      exposure:
        include: 'health,info,prometheus'

